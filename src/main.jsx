import React from "react";
import ReactDOM from "react-dom/client";
import PrayerFeedApp from "./PrayerFeedApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PrayerFeedApp />
  </React.StrictMode>
);
